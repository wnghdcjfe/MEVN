"use strict";

var curry = require("./curry.js");

var takeAll = require("./takeAll.js");

var splitEveryLazy = require("../Lazy/splitEveryLazy.js");

module.exports = curry(function splitEvery(n, str) {
  return takeAll(splitEveryLazy(n, str));
});