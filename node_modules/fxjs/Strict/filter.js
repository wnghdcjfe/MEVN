"use strict";

var filterLazy = require("../Lazy/filterLazy.js");

var curry = require("./curry.js");

var takeAll = require("./takeAll.js");

module.exports = curry(function filter(f, iter) {
  return takeAll(filterLazy(f, iter));
});