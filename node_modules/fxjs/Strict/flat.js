"use strict";

var takeAll = require("./takeAll.js");

var flatLazy = require("../Lazy/flatLazy.js");

module.exports = flat;

function flat(iter, depth = 1) {
  return takeAll(flatLazy(iter, depth));
}