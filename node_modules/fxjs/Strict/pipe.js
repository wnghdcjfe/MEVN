"use strict";

require("core-js/modules/es.array.iterator");

var reduce = require("./reduce.js");

var go1Sync = require("../.internal/go1Sync.js");

module.exports = pipe;

function pipe(f, ...fs) {
  return (...as) => reduce(go1Sync, f(...as), fs);
}