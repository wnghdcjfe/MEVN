"use strict";

var curry = require('./curry.js');

var go = require('./go.js');

var takeAll = require('./takeAll.js');

var intersectionLazy = require('../Lazy/intersectionLazy.js');

module.exports = curry(function intersection(a, b) {
  return go(b, intersectionLazy(a), takeAll);
});