"use strict";

var curry = require("./curry.js");

var takeAll = require("./takeAll.js");

var chunkLazy = require("../Lazy/chunkLazy.js");

module.exports = curry(function chunk(n, iter) {
  return takeAll(chunkLazy(n, iter));
});