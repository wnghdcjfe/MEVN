"use strict";

require("core-js/modules/es.array.iterator");

var curry = require("./curry.js");

var object = require("./object.js");

var rejectLazy = require("../Lazy/rejectLazy.js");

var mapLazy = require("../Lazy/mapLazy.js");

module.exports = curry(function pick(ks, obj) {
  return object(rejectLazy(([_, v]) => v === undefined, mapLazy(k => [k, obj[k]], ks)));
});