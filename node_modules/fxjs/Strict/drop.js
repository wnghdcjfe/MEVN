"use strict";

var curry = require("./curry.js");

var takeAll = require("./takeAll.js");

var dropLazy = require("../Lazy/dropLazy.js");

module.exports = curry(function drop(l, iter) {
  return takeAll(dropLazy(l, iter));
});