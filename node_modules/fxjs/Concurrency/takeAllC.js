"use strict";

var takeAll = require("../Strict/takeAll.js");

var takeAllLazyC = require("../Lazy/takeAllLazyC.js");

var takeC = require("./takeC.js");

module.exports = takeAllC;

function takeAllC(n, iter) {
  return arguments.length > 1 ? takeAll(takeAllLazyC(n, iter)) : typeof n == 'number' ? _ => takeAllC(n, _) : takeC(Infinity, n);
}