"use strict";

require("core-js/modules/es.array.iterator");

var isIterable = require("../Strict/isIterable.js");

var entriesLazy = require("../Lazy/entriesLazy.js");

var mapEntriesLazy = require("../Lazy/mapEntriesLazy.js");

const baseCalls = (map, object) => function calls(fs, ...args) {
  return isIterable(fs) ? map(f => f(...args), fs) : object(mapEntriesLazy(f => f(...args), entriesLazy(fs)));
};

module.exports = baseCalls;