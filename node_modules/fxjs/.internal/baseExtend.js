"use strict";

var reduce = require("../Strict/reduce.js");

var mapLazy = require("../Lazy/mapLazy.js");

var entriesLazy = require("../Lazy/entriesLazy.js");

module.exports = baseExtend;

function baseExtend(set, obj, objs) {
  const type = typeof obj;
  obj && (type == 'object' || type == 'function') && reduce(reduce(set), obj, mapLazy(entriesLazy, objs));
  return obj;
}