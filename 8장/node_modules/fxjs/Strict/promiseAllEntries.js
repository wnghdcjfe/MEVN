"use strict";

var identity = require("./identity.js");

var mapEntries = require("./mapEntries.js");

module.exports = promiseAllEntries;

function promiseAllEntries(entries) {
  return mapEntries(identity, entries);
}